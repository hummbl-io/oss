[build-system]
requires = ["setuptools>=68.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "hummbl-cognition"
version = "0.1.0"
description = "Cognitive Ledger Protocol (CLP) and Open Brain server for HUMMBL agent reasoning"
readme = "README.md"
license = {text = "Apache-2.0"}
requires-python = ">=3.11"
authors = [{ name = "HUMMBL, LLC", email = "dev@hummbl.io" }]
keywords = ["cognition", "ledger", "clp", "open-brain", "reasoning", "hummbl", "ai", "agents"]
classifiers = [
    "Development Status :: 3 - Alpha",
    "Intended Audience :: Developers",
    "License :: OSI Approved :: Apache Software License",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Programming Language :: Python :: 3.13",
    "Topic :: Software Development :: Libraries :: Python Modules",
    "Topic :: Scientific/Engineering :: Artificial Intelligence",
]
dependencies = [
    "hummbl-bus>=0.1.0",
]

[project.urls]
Homepage = "https://github.com/hummbl-io/oss"
Documentation = "https://github.com/hummbl-io/oss/tree/main/packages/python/hummbl-cognition/README.md"
Repository = "https://github.com/hummbl-io/oss"
Issues = "https://github.com/hummbl-io/oss/issues"
PyPI = "https://pypi.org/project/hummbl-cognition/"

[project.optional-dependencies]
governance = [
    "hummbl-governance>=1.2.2",
]
test = [
    "pytest>=7.0",
    "pytest-timeout>=2.3",
    "coverage>=7.0",
    "mypy>=1.8",
    "jsonschema>=4.0",
]

[tool.setuptools.packages.find]
where = ["src"]
include = ["hummbl_cognition*"]

[tool.setuptools.package-dir]
"" = "src"

[tool.setuptools.package-data]
hummbl_cognition = ["schemas/*.json", "seed_registries/*.jsonl", "data/*.json"]

[tool.pytest.ini_options]
pythonpath = ["src"]
testpaths = ["tests"]
markers = [
    "allow_ledger_writes: mark test to allow writing to ledger test artifacts",
]

[project.scripts]
hummbl-cognition = "hummbl_cognition.__main__:main"

[tool.ruff.lint]
select = ["E", "F", "W", "I"]
ignore = ["E501"]
