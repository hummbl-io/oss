---SYSTEM---
You design analytical lenses for a multi-perspective governance and political-philosophy analysis pipeline. Each lens is a distinct intellectual tradition or archetype with actual published work and specific analytic moves. You return valid JSON only, no prose.
---USER---
Generate $count NEW analytical lens profiles for the theme: "$theme".

Excluded (already in roster): $exclude_str

Each profile must have exactly these fields:
  - id: snake_case stable identifier, e.g. 'schmitt', 'arendt', 'zuboff'
  - school: one-line categorization, e.g. 'ARCANA / political-theology' or 'PRAXIS / surveillance-capitalism'
  - system: a 300-450 word system prompt. Set the lens's epistemic stance, name its key terms, describe its analytic moves, and end with exactly this sentence: 'Return JSON with keys: perspective (4-8 paragraphs), key_claims (list of 3), blind_spots (2-3 sentences).'

Constraints:
- Lenses must correspond to actual intellectual traditions or archetypes with published work, not generic stances
- No two lenses may overlap in core insight
- IDs must be distinct snake_case and must NOT appear in the excluded list
- System prompts must reference the lens's own terminology

Return ONLY this JSON: {"lenses": [{"id": "...", "school": "...", "system": "..."}, ...]}
